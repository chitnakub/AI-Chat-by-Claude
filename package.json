{
  "name": "airs-gw-chat-ui",
  "version": "1.0.0",
  "description": "AI chat UI calling Vertex AI via AIRS-GW, with switchable x-portkey-metadata (_user) and x-portkey-config headers",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
